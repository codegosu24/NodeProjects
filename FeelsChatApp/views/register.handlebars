{{#if errors}}
      {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
      {{/each}}
{{/if}}
<form method="post" action="/users/register">
<div class="form-group">
    <label for="email">Email address or Mobile Number:</label>
    <input type="email" class="form-control" name="email" id="email">
  </div>
  <div class="form-group">
    <label for="Name">Full Name</label>
    <input type="Name" class="form-control" name="name" id="email">
  </div>
  <div class="form-group">
    <label for="userame">Username</label>
    <input type="username" class="form-control" name="username" id="email">
  </div>
  <div class="form-group">
    <label for="pwd">Password:</label>
    <input type="password" class="form-control" name="password" id="pwd">
  </div>
  <button type="submit" onclick="signup(this.form)" style="margin-top:10px" class="btn btn-default">Sign Up</button>
</form>
<h1 style="text-align:center;font-family:Billabong;font-weight:bold;font-size:20px;padding-top:20px">Have an account?<a href="/users/login"> Sign in</a></h1>

